# CHANGELOG

## 0.3.2 (2026-09-03)
- 版图解码移出主线程与单一 actor 队列，主版图和缩略图使用独立的有界优先队列；快速换版不再等待已取消缩略图，并保留同键合并、取消隔离与真实图片/PDF 回退
- 已缓存的同版低清缩略图会立即作为主图占位，高清解码完成后原位替换；缓存严格绑定文件修订和版次，避免换版时闪白或串图
- OCR 缺省结构块在后台载荷构建阶段一次生成，证据区与正文块改为惰性布局，移除逐块重复全文的无障碍字符串；Logo 与 Banner 复用内存缓存
- 八报抓取改为默认 4 路跨来源并发，单一报纸内部的 acquire→fetch→parse 与整期原子提交仍保持串行；可用 `--workers 1..8` 调整
- 批次协调锁与来源证据锁分离：同日重复抓取仍被拒绝；普通详情、收件箱和日报面板遇到正在更新的来源会立即返回状态，不再排队等待，强校验、草稿和发布仍保持互斥
- 工作台将整批抓取与读报加载拆成独立状态；抓取期间仍可切换同日其他报纸、换版、阅读原版、缩放和编辑，完成回载保留当前报纸、当前版次及期间产生的编辑
- 普通详情读取使用持久化元数据修订标识，避免每次切报读取并哈希全部版图/PDF；旧草稿首次强校验后自动升级，保存与发布继续执行完整内容摘要校验
- 并发 worker 复用已固定归档身份，锁根跨 GUI/终端/launchd 保持一致；并发创建目录、大小写路径别名、目录替换、致命路径异常和日志追加均增加回归保护
- HTTP 连接池改为单来源线程局部复用，科技日报 POST 进入同一连接池；Mac 客户端直接启动包内抓取器，减少一层 Python 进程

## 0.3.1 (2026-09-03)
- 切换报纸改为可取消的“最后一次选择生效”加载，正文读取与阅读状态写回解耦，加载期间仍可继续切换
- 新增 8 期 LRU 内存缓存；保存或预览后主动失效当前缓存，兼顾重复切换速度与后端派生告警一致性
- macOS 客户端直接调用工作台 API，后端按命令延迟导入重模块；真实归档 24 轮交错对照中整期加载平均减少 60.6%，仪表盘减少 65.8%
- 收件箱/仪表盘概览避免构造完整 OCR 文章块和无条件哈希整期证据；存在草稿或已发布建设报时仍保留强证据校验
- 阅读状态采用乐观显示、后台落盘和版本化合并；旧仪表盘响应、快速连点及手动完成与自动 opened 竞态不会回滚已确认状态
- 切报加载态统一覆盖左栏、中栏、右侧工作区和无障碍提示，读完计数始终采用当前显示状态
- 大型整期载荷在后台完成结构映射，OCR 较多的报纸不会占用主线程；迟到的旧阅读状态响应不会覆盖较新的跨端状态
- 归档读写固定到同一目录句柄，跨进程锁按目录 inode 识别，并核验导入原始 PDF 的实际 SHA-256；既有未篡改草稿与发布摘要保持兼容
- 新增导航、缓存、失败恢复、并发状态和后端完整性回归；发布基线为 81 项 Swift 测试与 316 项 Python 测试

## 0.3.0 (2026-09-03)
- 原生 macOS 应用统一命名为 Read Daily，并采用 `assets/logo/readdaily-icon.svg` 生成 App 图标
- 固定 8 报按日期与中央党报/部委行业报/地方党报分类，缺报不借用旧数据
- 新增真实版面缩略图、原版上/OCR 下证据区、25%–400% 缩放预览和独立 OCR 校对字段
- 新增三张本地 SVG Banner 轮播、日期/天气信息、每日获取与阅读状态、右侧中文摘要工作栏
- 八报均可保存本地草稿；正式 Obsidian 发布仍严格限定中国建设报并保留预览、冲突检测与回滚
- Release 内置最小 Python 运行时脚本、macOS 13 arm64 OCR helper、许可证及固定哈希的微信下载组件，不依赖另行克隆仓库或开发机 HOME 副本
- 新增版本化 ZIP、SHA-256 与独立分发验收脚本；当前仍为 Apple Silicon、ad-hoc 签名、未公证包
- 增加跨日期/重定向强校验、同日抓取锁、唯一原子临时文件与全入口 archive–Vault 隔离，避免旧期或并发任务污染当天证据
- 版次改用来源真实编号并要求连续唯一；所有已发现文章完整归版，任何缺页、孤立链接或请求失败均整期拒绝落盘
- 移除文章数量、HTML 窗口、段落数与正文长度的静默裁剪，并增加 PDF 全页清单、报头日期和 OCR 失败原子性校验
- 构建在编译前后绑定源码清单，阻止旧二进制与新源码混装进入 Release
- 草稿支持逐版增量保存，OCR 按文章块保留标题/顺序/空白；发布预览绑定编辑版本，路径设置改为暂存后应用
- 补齐浅色对比度、轮播暂停/VoiceOver 语义、动态提示播报及 980 点响应式工作区
- 网络版图必须经过系统 ImageIO 或安全回退解码器的真实像素解码；伪造图片头、截断图及无法解码的版面均拒绝入库
- 同一来源/日期的抓取与解析改为整期原子刷新；失败会完整恢复旧期，成功刷新则使旧摘要与发布状态失效
- 发布计划同时绑定归档证据摘要与当前持久草稿摘要，创建、应用、回滚统一锁序，避免并发保存、抓取或回滚发布旧内容
- Vault 发布采用独立于 archive/Vault 的跨归档持久事务哨兵；关键原子替换同步父目录，支持异常中断后的受控续作或恢复
- 中国建设报日报使用“日期 + 中文报纸名”稳定路径，期号修正只更新受管内容，不制造重复知识卡片
- 本地 PDF 导入先制作并校验稳定快照，OCR、归档文件与来源哈希始终绑定同一字节版本
- Archive、Vault 与 publisher-state 从原始配置路径逐级 `O_NOFOLLOW` 固定目录句柄；所有公开写入口在同一会话内完成隔离校验和相对路径提交，阻断校验后的祖先换链
- 发布、回滚与异常恢复增加内容级 CAS、固定句柄事务枚举和目录 `fsync`；计划、快照、状态与持久哨兵不再重新解析可变绝对路径
- 通用版图真实解码门槛校准为短边 1000、长边 1400 像素，高分辨率渠道保留更高门槛；Founder/CMS 保留短段落及长短标题

## 0.2.2 (2026-09-02)
- 品牌资产：ReadDaily App 图标与字标「晨报之窗」系列（SVG/PNG 全尺寸 + 设计说明）

## 0.2.1 (2026-09-02)
- 北京日报贯通：数字报专用域（bjrbdzb.bjd.com.cn）移动版 innerHTML 化 → 新适配器 `mobile_epaper`（版次/全文/版面图，同系统晚报/副中心报可复用）
- 注册表：环球时报按用户决策移除（dropped）；8 家全部启用
- 当日全集：8 源 87→99 条目，跨报摘要含北京日报 12 版

## 0.2.0 (2026-09-02)
- 新增 `readdaily all` 无人值守模式：OpenAI 兼容 LLM 逐篇归纳（严格 JSON schema + 校验重试）
- 每日摘要 LLM 生成（跨报主题块）+ 主体自动跟踪（top 实体）
- 新增 launchd 任务 com.guopeijun.readdaily-llm（10:50/20:15）
- 配置：llm.json（chmod 600，不入仓库）；README 已更新

## 0.1.0 (2026-09-02)
- 首次开源：4 类渠道适配器（wechat_read/founder/paper_api/cms_index），注册表驱动
- 7 家报纸实测全通（人民日报/光明日报/经济日报/科技日报/农民日报/南方日报/中国建设报）
- 归纳流水线（LLM schema + 校验门）→ Obsidian 归档（报纸原文/每日摘要/主体档案/看板）
- 主题检索 `readdaily query` + 主体跟踪 `readdaily tracking`
- launchd 定时（10:25/20:00 自动抓取）；install.sh 幂等安装（多端技能软链 + OCR 构建）
